[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/api/*"
  to = "https://ai-ui-generator-1.onrender.com/api/:splat"
  status = 200
  force = true
